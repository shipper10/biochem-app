import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oraby.biochemqa',
  appName: 'Oraby Biochemistry QA',
  webDir: 'www'
};

export default config;
